# Tasks: Sudoku Web App

1. [x] Setup project structure and install dependencies (`clsx`, `tailwind-merge`, `lucide-react`).
2. [x] Define TypeScript types for game state and cells.
3. [x] Implement Sudoku engine (`sudoku.ts`) for generation and validation.
4. [x] Implement `useSudoku` hook for state management (timer, moves, notes, undo, reset).
5. [x] Build `SudokuCell` component with highlighting and notes support.
6. [x] Build `SudokuBoard` component with keyboard navigation.
7. [x] Build `Controls` component (numpad, actions).
8. [x] Build `Header` component (timer, difficulty, new game).
9. [x] Assemble `Game` component and integrate with `App`.
10. [x] Test gameplay, validation, and responsive design.
